# Production (build + nginx): docker compose up --build  →  http://localhost:8080
# Dev (hot reload):         docker compose -f docker-compose.dev.yml up --build  →  http://localhost:5173

services:
  web:
    build: .
    ports:
      - "8080:80"
    restart: unless-stopped
